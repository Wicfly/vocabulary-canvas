# 🚀 GitHub Pages 部署步骤

## 快速开始

### 第一步：在 GitHub 上创建仓库

1. 访问 https://github.com/new
2. 仓库名称：`vocabulary-canvas`（或你喜欢的名字）
3. 选择 **Public**（GitHub Pages 免费版需要公开仓库）
4. **不要**勾选任何初始化选项
5. 点击 "Create repository"

### 第二步：连接本地仓库到 GitHub

在终端执行以下命令（替换 `YOUR_USERNAME` 为你的 GitHub 用户名）：

```bash
cd /Users/hope/Desktop/website

# 添加远程仓库
git remote add origin https://github.com/YOUR_USERNAME/vocabulary-canvas.git

# 推送到 GitHub
git branch -M main
git push -u origin main
```

### 第三步：启用 GitHub Pages

1. 进入你的 GitHub 仓库页面
2. 点击 **Settings**（设置）
3. 在左侧菜单找到 **Pages**
4. 在 "Source" 部分：
   - 选择 **GitHub Actions**
5. 点击 **Save**

### 第四步：更新仓库名称（重要！）

如果仓库名不是 `vocabulary-canvas`，需要更新 `vite.config.js`：

打开 `vite.config.js`，找到这一行：
```javascript
return '/vocabulary-canvas/' // 默认仓库名，需要根据实际仓库名修改
```

将 `vocabulary-canvas` 改为你的实际仓库名。

### 第五步：提交并推送

```bash
git add .
git commit -m "Configure for GitHub Pages"
git push
```

### 第六步：等待部署完成

1. 在 GitHub 仓库页面，点击 **Actions** 标签
2. 查看 "Deploy to GitHub Pages" 工作流
3. 等待部署完成（通常需要 1-2 分钟）
4. 部署成功后，访问：`https://YOUR_USERNAME.github.io/vocabulary-canvas/`

---

## 部署后的 URL

你的网站地址将是：
```
https://YOUR_USERNAME.github.io/vocabulary-canvas/
```

记得将：
- `YOUR_USERNAME` 替换为你的 GitHub 用户名
- `vocabulary-canvas` 替换为你的实际仓库名

---

## 自动部署

配置完成后，每次你执行：
```bash
git add .
git commit -m "Your message"
git push
```

GitHub Actions 会自动构建并部署你的网站！

---

## 常见问题

### 1. 页面显示 404

**解决方案：**
- 检查 `vite.config.js` 中的仓库名是否正确
- 确保 GitHub Pages 已启用并选择 "GitHub Actions" 作为源

### 2. 样式或资源加载失败

**解决方案：**
- 检查浏览器控制台的错误信息
- 确保所有资源路径正确
- 清除浏览器缓存后重试

### 3. 部署工作流失败

**解决方案：**
- 检查 GitHub Actions 日志查看具体错误
- 确保 `package.json` 中的依赖都正确
- 检查 Node.js 版本是否兼容

### 4. 如何更新网站

只需推送新代码即可：
```bash
git add .
git commit -m "Update website"
git push
```

GitHub Actions 会自动重新部署！

---

## 需要帮助？

如果遇到问题，可以：
1. 查看 GitHub Actions 的日志
2. 检查 `DEPLOY.md` 文件获取更多信息
3. 查看 GitHub Pages 文档：https://docs.github.com/en/pages

